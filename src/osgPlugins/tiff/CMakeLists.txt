INCLUDE_DIRECTORIES( ${TIFF_INCLUDE_DIR} )

SET(TARGET_SRC ReaderWriterTIFF.cpp )
set(TIFF_LIBRARIES
    $<$<CONFIG:Debug>:${TIFF_LIBRARY_DEBUG}>
    $<$<CONFIG:Release>:${TIFF_LIBRARY_RELEASE}>
)
SET(TARGET_LIBRARIES_VARS TIFF_LIBRARIES)

#### end var setup  ###
SETUP_PLUGIN(tiff)
