INCLUDE_DIRECTORIES( ${JPEG_INCLUDE_DIR} )

SET(TARGET_SRC ReaderWriterJPEG.cpp )
set(JPEG_LIBRARIES
    $<$<CONFIG:Debug>:${JPEG_LIBRARY_DEBUG}>
    $<$<CONFIG:Release>:${JPEG_LIBRARY_RELEASE}>
)
SET(TARGET_LIBRARIES_VARS JPEG_LIBRARIES )
#### end var setup  ###
SETUP_PLUGIN(jpeg)
